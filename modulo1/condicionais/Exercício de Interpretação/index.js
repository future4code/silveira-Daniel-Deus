/*

const respostaDoUsuario = prompt("Digite o número que você quer testar")
const numero = Number(respostaDoUsuario)

if (numero % 2 === 0) {
  console.log("Passou no teste.")
} else {
  console.log("Não passou no teste.")
}

(O código irá realizar uma verificação se o número é par ou não.)
=> Os números que forem par passaram no teste
=> e os que forem impar não passaram no teste

*/

/*

let fruta = prompt("Escolha uma fruta")
let preco
switch (fruta) {
  case "Laranja":
    preco = 3.5
    break;
  case "Maçã":
    preco = 2.25
    break;
  case "Uva":
    preco = 0.30
    break;
  case "Pêra":
    preco = 5.5
    break; // BREAK PARA O ITEM c.
  default:
    preco = 5
    break;
}
console.log("O preço da fruta ", fruta, " é ", "R$ ", preco)

O codigo acima está pedindo que o usuário selecione uma fruta, para descobrir o preço.
O valor de maçã  será 2.25

*/

/*

const numero = Number(prompt("Digite o primeiro número."))

if(numero > 0) {
  console.log("Esse número passou no teste")
	let mensagem = "Essa mensagem é secreta!!!"
}

console.log(mensagem)


Está pedindo para o usuario digitar um numero, e estamos transformando a string em tipo number
dependendo da escolha, caso seja superior a 0, retornará que o numero passou no teste.
e virse versa caso seja -

*/

